export interface SessionTerm{
    sessionId: string; 
    sessionTermId: string; 
    termName: string;
    schoolId: string;
    startDate: string;
    endDate: string;
    term?: string;
    sessionKey?: string;
}